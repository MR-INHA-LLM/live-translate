# Models & weights (multi-GB — never commit)
# 루트 한정(/models/) — app/models/ 소스 패키지는 추적해야 한다.
/models/
*.safetensors
*.gguf
!app/models/

# Eval data & generated hypotheses (re-downloadable / re-generatable)
data/

# Python / uv
.venv/
__pycache__/
*.pyc
.python-version

# Logs (all logs live under logs/ to keep the root clean)
logs/
*.log

# Env
.env
